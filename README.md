# dotfiles

My personal configs

# Usage (WIP)

```bash
$ dconf dump /org/gnome/terminal/ > gnome-terminal.conf
$ dconf load /org/gnome/terminal/ < gnome-terminal.conf

$ stow -t ~ nvim
$ stow -t ~ i3
```

# TODO:

- .bashrc
